### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Ad {
  adId: Int!
  adminCreator: Admin
  adminId: Int!
  category: Category
  categoryId: Int!
  creationDate: DateTime!
  detail: String!
  keywords: [String!]!
  photos: [String!]!
  publishedBy: Publisher
  publisherId: Int!
  title: String!
}

type Admin {
  adminId: Int!
  user: User
  userId: Int!
}

type AuthPayload {
  token: String!
  user: User!
}

type Category {
  categoryId: Int!
  title: String!
}

type Client {
  clientId: Int!
  user: User
  userId: Int!
}

"""
A date-time string at UTC, such as 2007-12-03T10:15:30Z, compliant with the `date-time` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.
"""
scalar DateTime

type Mutation {
  addAd(adminId: Int!, categoryId: Int, detail: String!, keywords: [String!]!, photos: [String!]!, publisherId: Int!, title: String!): Ad!
  addAdmin(userId: Int!): Admin!
  addCategory(title: String!): Category!
  addClient(userId: Int!): Client!
  addPublisher(cellphone: String!, photo: String!, userId: Int!): Publisher!
  addSuggestedCategory(title: String!): SuggestedCategory!
  addUser(email: String!, lastName: String!, name: String!, password: String!, userName: String!): User!
  login(email: String!, password: String!): AuthPayload!
  removeAd(adId: Int!): Ad!
  removeAdmin(adminId: Int!): Admin!
  removeCategory(categoryId: Int!): Category!
  removeClient(clientId: Int!): Client!
  removePublisher(publisherId: Int!): Publisher!
  removeSuggestedCategory(suggestedCategoryId: Int!): SuggestedCategory!
  removeUser(userId: Int!): User!
  signup(email: String!, lastName: String!, name: String!, password: String!, userName: String!): AuthPayload!
  updatePublisher(cellphone: String, photo: String, publisherId: Int!): Publisher!
  updateUser(email: String, lastName: String, name: String, userId: Int!, userName: String): User!
}

type Publisher {
  cellphone: String!
  photo: String!
  publisherId: Int!
  user: User
  userId: Int!
}

type Query {
  SearchCategoryByTitle(categoryTitle: String!): Category
  ad(adId: Int!): [Ad!]!
  admin(userId: Int!): [Admin!]!
  admins: [Admin!]!
  ads: [Ad!]!
  categories: [Category!]!
  category(categoryId: Int!): [Category!]!
  client(userId: Int!): [Client!]!
  clients: [Client!]!
  publisher(userId: Int!): [Publisher!]!
  publishers: [Publisher!]!
  search(text: String!): [Ad]!
  suggestedCategories: [SuggestedCategory!]!
  suggestedCategory(suggestedCategoryId: Int!): [SuggestedCategory!]!
  user(userId: Int!): [User!]!
  users: [User!]!
}

type SuggestedCategory {
  suggestedCategoryId: Int!
  title: String!
}

type User {
  email: String!
  lastName: String!
  name: String!
  password: String!
  userId: Int!
  userName: String!
}
